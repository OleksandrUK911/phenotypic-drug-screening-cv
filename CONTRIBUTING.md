# Contributing

This is currently a solo portfolio project. Code style: `black` + `ruff` + `isort` (enforced via pre-commit,
see `.pre-commit-config.yaml`). Type hints checked with `mypy` on a best-effort basis. Open an issue before
a PR if you'd like to propose a change.
